# 01_エンジニア共有用

## 読む順番

1. **MVP実装仕様書.md** — いま動いているものは何か、封じ込めているものは何か、封じ込めはどう実装されているか
2. **フェーズ別機能ロードマップ.md** — Phase 1〜4 で何を追加するか、技術タスク単位で

## 誰が見る

- 社内のエンジニア（実装・リリース担当）
- 外部の協力エンジニア（レビュー・引き継ぎ）

## 更新ルール

- `middleware.ts` の `HIDDEN_PATH_PREFIXES` を変更したら、この2ファイルも必ず同期して更新
- Phase 移行時は `フェーズ別機能ロードマップ.md` の対応行を「完了」に移動
